"""Use cases. Imports `domain` only.

The settlement evaluation and the adjudication entry points live here; the layer exists now,
empty, so the `import-linter` contracts are enforced from the first commit rather than bolted
on once there is code to violate them.
"""

__all__: list[str] = []
